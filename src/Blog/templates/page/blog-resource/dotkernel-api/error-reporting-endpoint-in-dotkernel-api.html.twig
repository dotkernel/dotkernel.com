{% extends '@layout/default.html.twig' %}

{% block title %}Error reporting endpoint in Dotkernel API{% endblock %}

{% block page_title %}
    <div class="mb-2 text-uppercase small fw-bold text-primary tracking-wider">Dotkernel API</div>
    <h1 class="fw-bold text-dark display-6 mb-0">Error reporting endpoint in Dotkernel API</h1>
    <hr class="mt-4 mb-2 opacity-25">
{% endblock %}

{% block content %}
    <div class="container py-4">
        <div class="row g-4">

            {{ include('@partial/left-menu.html.twig') }}

            <main class="col-12 col-md-8 col-lg-9">
                <article class="card border-0 shadow-sm p-4 p-md-5 h-100">
                    <header class="mb-4">
                        <h1 class="h2 text-dark fw-bold">Error reporting endpoint in Dotkernel API</h1>
                        <div class="text-muted small">
                            <i class="bi bi-folder2-open me-1"></i> Dotkernel API
                        </div>
                    </header>

                    <section class="post-content text-secondary lh-lg fs-6">
                        <!-- wp:heading {"level":1} -->
<h1 class="wp-block-heading"></h1>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">Dotkernel API has received a lot of love from our developers, with regular updates to the platform for years. We use Dotkernel API in our projects, so any bugs and issues are addressed as soon as they are found. Still, it's not unlikely that some hidden issues remain in fringe use cases that we simply haven't explored. The occurrence of bugs increases when the API is used in a complex frontend project.</p>
<!-- /wp:paragraph -->

<!-- wp:paragraph -->
<p class="mb-3">Fatal errors are easily found in the API logs, but it's another matter altogether to deal with incorrect data processing that doesn't generate errors in the frontend that interfaces with the API. The error reporting endpoint was designed to allow the <strong>frontend developers</strong> of your API to report any bugs they encounter in a secure way that is fully under your control.</p>
<!-- /wp:paragraph -->

<!-- wp:heading {"level":4} -->
<h4 class="wp-block-heading">Example case usage</h4>
<!-- /wp:heading -->

<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li>Frontend developed in Angular.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li>Frontend developer will use try-catch in the code in order to send <strong>frontend errors</strong> back to the API.</li>
<!-- /wp:list-item --></ul>
<!-- /wp:list -->

<!-- wp:heading -->
<h2 class="wp-block-heading">How to use it on the API side</h2>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">Error reporting is done by sending a <strong>POST</strong> request to the <code>/error-report</code> endpoint, together with a <strong>token</strong> in the header. In the sections below we will detail how to configure error reporting in your API and how the endpoint is used by the frontend developers.</p>
<!-- /wp:paragraph -->

<!-- wp:heading {"level":4} -->
<h4 class="wp-block-heading">Generating a token and adding it to your API config</h4>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">First you need to generate a token for your request. This is done by using the below command.</p>
<!-- /wp:paragraph -->

<!-- wp:enlighter/codeblock {"language":"bash"} -->
<pre class="EnlighterJSRAW" data-enlighter-language="bash" data-enlighter-theme="" data-enlighter-highlight="" data-enlighter-linenumbers="" data-enlighter-lineoffset="" data-enlighter-title="" data-enlighter-group="">php ./bin/cli.php token:generate error-reporting</pre>
<!-- /wp:enlighter/codeblock -->

<!-- wp:paragraph -->
<p class="mb-3">The resulting token has this format <code>0123456789abcdef0123456789abcdef01234567</code>.<br><strong>Note:</strong> this example is provided just to let you know what to look for.</p>
<!-- /wp:paragraph -->

<!-- wp:paragraph -->
<p class="mb-3">Copy the generated token in your <code>config/autoload/error-handling.global.php</code> file. It should look similar to the example below. Your API can have multiple tokens, if needed.</p>
<!-- /wp:paragraph -->

<!-- wp:enlighter/codeblock -->
<pre class="EnlighterJSRAW" data-enlighter-language="generic" data-enlighter-theme="" data-enlighter-highlight="" data-enlighter-linenumbers="" data-enlighter-lineoffset="" data-enlighter-title="" data-enlighter-group="">return ,
        ...
    ]
]</pre>
<!-- /wp:enlighter/codeblock -->

<!-- wp:heading {"level":4} -->
<h4 class="wp-block-heading">Validation mechanism</h4>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">Behind the scenes, the API validates your configuration and lets you know if any config items prevent the submission of the error report. Below are the requirements for an application to be able to send error messages to Dotkernel API.</p>
<!-- /wp:paragraph -->

<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li><strong>Server-side requirements</strong> stored in <code>config/autoload/error-handling.global.php</code> (these can be set/overwritten in `config/autoload/local.php`):<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li>All keys (<code>enabled</code>, <code>path</code>, <code>tokens</code>, <code>domain_whitelist</code> and <code>ip_whitelist</code>) must exist under <code>ErrorReportServiceInterface::class</code>.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li>The error reporting feature must be enabled by setting <code>ErrorReportServiceInterface::class</code> . <code>enabled</code> to <code>true</code>.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li><code>ErrorReportServiceInterface::class</code> . <code>path</code> must have a value; if the destination file does not exist, it will be created automatically.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li><code>ErrorReportServiceInterface::class</code> . <code>tokens</code> must contain at least one token.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li>At least one of <code>ErrorReportServiceInterface::class</code> . <code>domain_whitelist</code>/<code>ip_whitelist</code> must have at least one value.</li>
<!-- /wp:list-item --></ul>
<!-- /wp:list --></li>
<!-- /wp:list-item --></ul>
<!-- /wp:list -->

<!-- wp:paragraph -->
<p class="mb-3"><strong>Note:</strong> In <code>src/App/src/Service/ErrorReportService.php</code>, the method <code>checkRequest()</code> tries to validate the request by checking matches for <code>domain_whitelist</code> with <code>isMatchingDomain()</code> and for <code>ip_whitelist</code> with <code>isMatchingIpAddress()</code>.<br>If both return <code>false</code>, a <code>ForbiddenException</code> is thrown and the error message does not get stored.</p>
<!-- /wp:paragraph -->

<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li><strong>Application-side requirements</strong>:<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li>Send the <code>Error-Reporting-Token</code> header with a valid token previously stored in <code>config/autoload/error-handling.global.php</code> in the <code>ErrorReportServiceInterface::class</code> . <code>tokens</code> array.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li>Send the <code>Origin</code> header set to the application's URL; this is the application that sends the error message.</li>
<!-- /wp:list-item --></ul>
<!-- /wp:list --></li>
<!-- /wp:list-item --></ul>
<!-- /wp:list -->

<!-- wp:paragraph -->
<p class="mb-3"><strong>Note:</strong></p>
<!-- /wp:paragraph -->

<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li>The tokens under <code>ErrorReportServiceInterface::class</code> . <code>tokens</code> do not expire.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li>The log file stores the token value too, making it easy to identify which application sent the error message.</li>
<!-- /wp:list-item --></ul>
<!-- /wp:list -->

<!-- wp:paragraph -->
<p class="mb-3">If your request passes all the checks, the message is saved in the log file specified in <code>ErrorReportServiceInterface::class</code> . <code>path</code>.</p>
<!-- /wp:paragraph -->

<!-- wp:heading {"level":5} -->
<h5 class="wp-block-heading">Tips and tricks</h5>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">If there are multiple applications that report errors to your API, you can <strong>assign a different error reporting token</strong> for each. The tokens support key-value pairs where:</p>
<!-- /wp:paragraph -->

<!-- wp:list -->
<ul class="wp-block-list"><!-- wp:list-item -->
<li>The <strong>key</strong> is an alias relevant to the assigned application that uses it.</li>
<!-- /wp:list-item -->

<!-- wp:list-item -->
<li>The <strong>value</strong> is the token itself.</li>
<!-- /wp:list-item --></ul>
<!-- /wp:list -->

<!-- wp:paragraph -->
<p class="mb-3">Example:</p>
<!-- /wp:paragraph -->

<!-- wp:enlighter/codeblock {"language":"php"} -->
<pre class="EnlighterJSRAW" data-enlighter-language="php" data-enlighter-theme="" data-enlighter-highlight="" data-enlighter-linenumbers="" data-enlighter-lineoffset="" data-enlighter-title="" data-enlighter-group="">// ...
return ,
    ],
];</pre>
<!-- /wp:enlighter/codeblock -->

<!-- wp:paragraph -->
<p class="mb-3">The log file will have entries similar to the below:</p>
<!-- /wp:paragraph -->

<!-- wp:quote -->
<blockquote class="wp-block-quote"><!-- wp:paragraph -->
<p class="mb-3">  Demo error message</p>
<!-- /wp:paragraph --></blockquote>
<!-- /wp:quote -->

<!-- wp:paragraph -->
<p class="mb-3">The inclusion of the token helps you identify the source of the error message. In our example, it's the application that uses the <code>0123456789abcdef0123456789abcdef01234567</code> token, which is assigned to the application <code>frontend</code>.</p>
<!-- /wp:paragraph -->

<!-- wp:heading -->
<h2 class="wp-block-heading">How to use it on the Frontend side (Angular example)</h2>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">The API developer sends a generated token to the frontend developer who will save it in their <code>environment.staging.ts</code> and/or <code>environment.prod.ts</code>. From then on, it's the frontend developer's job to set up an error reporting function similar to the one below.</p>
<!-- /wp:paragraph -->

<!-- wp:enlighter/codeblock {"language":"js"} -->
<pre class="EnlighterJSRAW" data-enlighter-language="js" data-enlighter-theme="" data-enlighter-highlight="" data-enlighter-linenumbers="" data-enlighter-lineoffset="" data-enlighter-title="" data-enlighter-group="">postError(body: object): Promise&lt;any> {
     return new Promise((resolve, reject) => {
      return this.http.post(API_ENDPOINT + 'error-report', body , {headers: new HttpHeaders({'Error-Reporting-Token': 'TOKEN', 'Origin': 'https://example.com'})})).subscribe({
        next: (response: any) => {
          resolve(response);
        },
        error: (e: HttpErrorResponse) => reject(e),
        complete: () => console.info('Error on sending error'),
      });
    });
  }</pre>
<!-- /wp:enlighter/codeblock -->

<!-- wp:paragraph -->
<p class="mb-3">Whenever an error is found, the frontend will call <code>postError()</code> with a relevant description under <code>message</code>.</p>
<!-- /wp:paragraph -->

<!-- wp:enlighter/codeblock {"language":"js"} -->
<pre class="EnlighterJSRAW" data-enlighter-language="js" data-enlighter-theme="" data-enlighter-highlight="" data-enlighter-linenumbers="" data-enlighter-lineoffset="" data-enlighter-title="" data-enlighter-group="">apiService.postError({message: 'ERROR MESSAGE'})</pre>
<!-- /wp:enlighter/codeblock -->

<!-- wp:heading -->
<h2 class="wp-block-heading">Conclusion</h2>
<!-- /wp:heading -->

<!-- wp:paragraph -->
<p class="mb-3">The error reporting feature in Dotkernel API is a secured and highly configurable tool for users of your API to report any unwanted behavior. More often than not, a detailed error report will help developers understand how to replicate the issue and fix it in due course.</p>
<!-- /wp:paragraph -->

<!-- wp:paragraph -->
<p class="mb-3">This article is also included in the full API documentation <a href="https://docs.dotkernel.org/api-documentation/v5/core-features/error-reporting" target="_blank" rel="noreferrer noopener">https://docs.dotkernel.org/api-documentation/v5/core-features/error-reporting</a>.</p>
<!-- /wp:paragraph -->
                    </section>
                </article>
            </main>

        </div>
    </div>
{% endblock %}
