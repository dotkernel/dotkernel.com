{% extends '@layout/default.html.twig' %}
{% block json_ld %} {{ include('@jsonld/dotkernel/camelcase-table-names-in-mysql-on-windows.jsonld.twig') }} {% endblock %}

{% block title %}camelCase Table Names in MySQL on Windows{% endblock %}

{% block page_title %}
    <div class="wrap">
        <div class="post-hero">
            <a href="{{ url('page::blog') }}" class="breadcrumb-back">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
                Back to Blog
            </a>
            <div><span class="eyebrow">Dotkernel</span></div>
            <h1>{{ article.title }}</h1>
            <div class="post-meta">
                <span class="text-muted small">
                    By <a href="{{ path('page::author-resource', {slug: article.author.slug}) }}" class="fw-bold link-dark text-decoration-none">{{ article.author.name }}</a>
                    &nbsp;.&nbsp; {{ article.postDate|date('M d, Y') }}
                </span>
            </div>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="wrap post-body-wrap">
        <div class="post-layout">

            {{ include('@partial/left-menu.html.twig') }}

            <article class="article">
                <div class="entry">
If you are using a WAMP stack, like WAMP or XAMPP, and try to create a table in camelCase ( example: <strong>adminLogin</strong>)  you will notice that camelCase is not working, table name will be lowercase: <strong>adminlogin</strong>.
In order to fix this, you need to add to your my.cnf file the line:
<pre>>lower_case_table_names=2</pre>
and restart mysql.

More on that here: <a href="http://dev.mysql.com/doc/refman/4.1/en/server-system-variables.html#sysvar_lower_case_table_names" target="_blank" rel="noopener noreferrer">http://dev.mysql.com/doc/refman/4.1/en/server-system-variables.html#sysvar_lower_case_table_names</a>

<h2 id="user-content-faq">Frequently Asked Questions</h2>

<div class="faq-list">
    <details class="acc" open>
        <summary>What happens when you create a camelCase table name on WAMP or XAMPP? <span class="chev">+</span></summary>
        <div class="acc-body">
            <p class="mb-0">A table created with a camelCase name, for example adminLogin, ends up stored as all lowercase, e.g. adminlogin, instead.</p>
        </div>
    </details>
    <details class="acc">
        <summary>How do you fix it? <span class="chev">+</span></summary>
        <div class="acc-body">
            <p class="mb-0">Add the line <code>lower_case_table_names=2</code> to your my.cnf file and restart MySQL.</p>
        </div>
    </details>
</div>
                </div>

                {{ include('@partial/post-nav.html.twig') }}
            </article>

        </div>
    </div>
{% endblock %}
