<script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "CollectionPage",
        "@id": "{{ app.baseUrl ~ path('page::authors') }}#webpage",
        "url": "{{ app.baseUrl ~ path('page::authors') }}",
        "name": "Authors",
        "description": "Browse Dotkernel blog posts by author.",
        "isPartOf": { "@id": "{{ app.baseUrl }}#website" },
        "mainEntity": {
            "@type": "ItemList",
            "itemListElement": [
                {% for author in authors %}{% if not loop.first %},{% endif %}
                {
                    "@type": "ListItem",
                    "position": {{ loop.index }},
                    "url": "{{ app.baseUrl ~ path('page::author-resource', {slug: author.slug}) }}",
                    "name": "{{ author.name }}"
                }
                {% endfor %}
            ]
        }
    }
</script>
