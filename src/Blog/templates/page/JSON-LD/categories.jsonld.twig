<script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "CollectionPage",
        "@id": "{{ app.baseUrl ~ path('page::categories') }}#webpage",
        "url": "{{ app.baseUrl ~ path('page::categories') }}",
        "name": "Categories",
        "description": "Browse Dotkernel blog posts by category.",
        "isPartOf": { "@id": "{{ app.baseUrl }}#website" },
        "mainEntity": {
            "@type": "ItemList",
            "itemListElement": [
                {% for category in categories %}{% if not loop.first %},{% endif %}
                {
                    "@type": "ListItem",
                    "position": {{ loop.index }},
                    "url": "{{ app.baseUrl ~ path('page::category-resource', {slug: category.slug}) }}",
                    "name": "{{ category.name }}"
                }
                {% endfor %}
            ]
        }
    }
</script>
